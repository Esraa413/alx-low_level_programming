#include "main.h"

/**
 * print_most_numbers - print num except 2 and 4
 * only using _putchar twice
 * return: always 0
 */

void print_most_numbers(void)
{
	int num;

	for (num = 0; num <= 9; unm++)
	{
		if (num == 2 || num == 4)
			_putchar(num + 48);
	}
	_putchar('\n');
}

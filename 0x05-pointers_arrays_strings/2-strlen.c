#include "main.h"
#include <stdio.h>
/**
 * string _strlen -a function that returns the length of a string.
 * @s:  provides a similar function
 * returns:  length
 */

int _strlen(char *s)
{
	int length;

	for (length = 0; *s != '\0'; s++)
	{
		++length
	}
}
